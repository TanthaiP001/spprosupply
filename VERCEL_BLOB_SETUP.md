# 📦 วิธีตั้งค่า Vercel Blob Storage สำหรับ File Upload

## ปัญหา

บน Vercel serverless environment ไม่สามารถใช้ file system (`fs/promises`) ได้ เพราะ:
- ไม่มี persistent file system
- ไฟล์จะหายไปเมื่อ function จบการทำงาน
- ต้องใช้ cloud storage แทน

## วิธีแก้ไข: ใช้ Vercel Blob Storage

### ขั้นตอนที่ 1: ติดตั้ง Package

Package ถูกติดตั้งแล้วใน `package.json`:
```json
"@vercel/blob": "^0.25.0"
```

รัน:
```bash
npm install
```

### ขั้นตอนที่ 2: ตั้งค่า Vercel Blob Storage

#### 2.1 สร้าง Blob Store ใน Vercel

1. ไปที่ **Vercel Dashboard** → Project ของคุณ
2. ไปที่แท็บ **"Storage"**
3. คลิก **"Create Database"** หรือ **"Add Storage"**
4. เลือก **"Blob"**
5. ตั้งชื่อ (เช่น: `sppro-uploads`)
6. เลือก Region (แนะนำ: `sin1` สำหรับประเทศไทย)
7. คลิก **"Create"**

#### 2.2 ตั้งค่า Environment Variable

Vercel จะสร้าง environment variable อัตโนมัติ:
- `BLOB_READ_WRITE_TOKEN` - Token สำหรับอ่าน/เขียนไฟล์

**ตรวจสอบว่า:**
1. ไปที่ **Settings** → **Environment Variables**
2. ตรวจสอบว่ามี `BLOB_READ_WRITE_TOKEN` หรือไม่
3. ถ้าไม่มี → Vercel จะสร้างให้อัตโนมัติเมื่อสร้าง Blob Store

### ขั้นตอนที่ 3: Deploy ใหม่

```bash
git add .
git commit -m "Add Vercel Blob Storage for file uploads"
git push
```

Vercel จะ deploy อัตโนมัติ

---

## ✅ ตรวจสอบ

หลังจาก deploy แล้ว:

1. **ทดสอบ Upload:**
   - ไปที่ `/admin/products`
   - ลอง upload รูปภาพ
   - ควรจะ upload สำเร็จ

2. **ตรวจสอบ URL:**
   - รูปภาพจะถูกเก็บใน Vercel Blob Storage
   - URL จะเป็น: `https://[blob-id].public.blob.vercel-storage.com/...`
   - รูปภาพจะแสดงได้ทันที

---

## 🔧 Troubleshooting

### Error: "BLOB_READ_WRITE_TOKEN is not set"

**สาเหตุ:** ยังไม่ได้สร้าง Blob Store หรือ environment variable ยังไม่ได้ตั้งค่า

**วิธีแก้:**
1. ไปที่ Vercel Dashboard → Storage
2. สร้าง Blob Store ใหม่
3. ตรวจสอบว่า `BLOB_READ_WRITE_TOKEN` ถูกสร้างอัตโนมัติ
4. Redeploy

### Error: "Unauthorized" หรือ "Forbidden"

**สาเหตุ:** Token ไม่ถูกต้องหรือหมดอายุ

**วิธีแก้:**
1. ไปที่ Vercel Dashboard → Storage
2. คลิกที่ Blob Store ของคุณ
3. ไปที่ Settings → Tokens
4. สร้าง token ใหม่
5. Copy token ไปตั้งค่าใน Environment Variables
6. Redeploy

### Error: "File too large"

**สาเหตุ:** ไฟล์ใหญ่เกิน 5MB

**วิธีแก้:**
- ลดขนาดไฟล์
- หรือเพิ่ม limit ใน code (ไม่แนะนำ)

---

## 💰 Pricing

Vercel Blob Storage มี free tier:
- **Free:** 1 GB storage, 100 GB bandwidth/month
- **Pro:** $0.15/GB storage, $0.40/GB bandwidth

สำหรับเว็บเล็กๆ free tier เพียงพอแล้ว

---

## 📝 หมายเหตุ

- ไฟล์ที่ upload จะถูกเก็บใน Vercel Blob Storage
- URL จะเป็น public URL ที่เข้าถึงได้ทันที
- ไม่ต้องกังวลเรื่อง file system หรือ directory structure
- ไฟล์จะไม่หายไปเมื่อ redeploy

---

## 🎯 สรุป

1. ✅ ติดตั้ง `@vercel/blob` package (ทำแล้ว)
2. ⏳ สร้าง Blob Store ใน Vercel Dashboard
3. ⏳ ตรวจสอบ `BLOB_READ_WRITE_TOKEN` environment variable
4. ⏳ Deploy ใหม่
5. ✅ ทดสอบ upload

